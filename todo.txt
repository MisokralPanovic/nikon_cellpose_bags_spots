- cluster compatible with load module
- bash for data manipulation, github cloning etc (rsync?)
- nextflow or snakemake


---
1. clone repo
2. copy raw data
- from dropbox
- from pi_data (more likely)
3. rename repo (according to raw data)
- add version if it was done before (or do this for final copy)
4. user adjusts config
***cluster snakemake
5. conda env
- check if exist and is identical
6. run pipeline
7. remove raw
- but write what it was (e.g. tree)
8. copy/send data
***
---

additional need:
-- script from usb to dropbox | pi/data
- those two should be synced
-- script for initial repo get



big different:
- cellpose in 3D
- newer versions of all
- bash files to be included in python


- automatic config detection
- experiment and data elucitated from config file location?
- unit check for making figures
- simplify unitcheck with conftest
- write log file + console calls
- cluster compatibility


---
code quality audit (2026-07-15, via Claude, see LEARNING_NOTES.md for details):

1. error handling & robustness   <- principles discussed, implementation pending
- per-file/per-scene failure isolation so one bad item doesn't kill the whole run
- replace silent broad except Exception with logged, contextual failures (utils.py, qc_figures.py)
- decide: catch+skip for data/hardware errors vs fail-fast for config/programmer errors
- fix qc_figures.py coor= typo (found via audit, ~line 328, only hit with 0-1 spots)
- try/except only at run_pipeline orchestration layer (_process_file/_process_scene calls), not inside
  segment_2d/segment_3d/measure_objects etc - those stay exception-transparent

2. logging & observability   <- working on this now
- replace print() with logging module: levels, timestamps, log file + console
- record which config produced which output CSV

3. cluster / parallelization readiness
- segmentation.use_gpu config flag is dead code, never read (utils.py hardcodes gpu=True)
- no checkpointing/resume for interrupted runs
- fully sequential file/scene loops, no parallel scheduling hooks for snakemake

4. testing coverage
- qc_figures.py (691 lines, largest module) has zero tests
- test_detection.py, test_model_bundle.py, test_run_pipeline.py are empty stubs

5. code organization
- config.py has no schema/validation, KeyErrors surface far from the actual mistake
- experiment name inferred from folder structure (run_pipeline.py), can silently end up empty
- obejct_measurement.py filename typo is existing/intentional, don't fix blindly
---
