- cluster compatible with load module
- bash for data manipulation, github cloning etc (rsync?)
- nextflow


---
1. clone repo
2. copy raw data
- from dropbox
- from pi_data (more likely)
3. rename repo (according to raw data)
- add version if it was done before (or do this for final copy)
4. user adjusts config
***cluster snakemake
5. conda env
- check if exist and is identical
6. run pipeline
7. remove raw
- but write what it was (e.g. tree)
8. copy/send data
***
---

additional need:
-- script from usb to dropbox | pi/data
- those two should be synced
-- script for initial repo get



big different:
- cellpose in 3D
- newer versions of all
- bash files to be included in python



- use bioio from inputs
- redo env to use less packages and most via conda
- train spotiflow on 3d with napari
    - make and label images
    - train and evaluate
    - evaluate on 2d
- cellpose stitch_treshold ~0.4 on 4x downsampeled images with new models
- stack - min zproject
- make it for 3d (flattened and normal) and 2d 
- rewrite the code