- cluster compatible with load module
- bash for data manipulation, github cloning etc (rsync?)
- nextflow or snakemake


---
1. clone repo
2. copy raw data
- from dropbox
- from pi_data (more likely)
3. rename repo (according to raw data)
- add version if it was done before (or do this for final copy)
4. user adjusts config
***cluster snakemake
5. conda env
- check if exist and is identical
6. run pipeline
7. remove raw
- but write what it was (e.g. tree)
8. copy/send data
***
---

additional need:
-- script from usb to dropbox | pi/data
- those two should be synced
-- script for initial repo get



big different:
- cellpose in 3D
- newer versions of all
- bash files to be included in python


- automatic config detection
- experiment and data elucitated from config file location?
- unit check for making figures
- simplify unitcheck with conftest
- write log file + console calls
- cluster compatibility


---
code quality audit (2026-07-15, via Claude, see LEARNING_NOTES.md for details):

1. error handling & robustness   <- DONE 2026-07-27 (mostly - see open items below), LEARNING_NOTES.md
- run_pipeline.py: (a) logger.exception fixed, now names scene/file/condition and says "skipping"
  (b) failures list/CSV added - single shared list threaded through _process_file as a parameter, so both
      the per-scene and per-file except blocks append to the same table -> _run_failures_{MODE}.csv,
      written unconditionally before the "no records -> return None" early-exit (that ordering was itself
      a bug during implementation: writing it after the early return meant a run where every file failed
      never got its failures CSV - fixed by moving the write above the check)
  (c) file-level try/except now wraps _process_file's call in run_pipeline - a corrupt file (BioImage()
      throwing) is now caught, logged, appended to failures, and the file loop continues
  (d) isolation tests added: TestProcessFile.test_handles_corrupted_scene, TestRunPipeline.test_handles_
      corrupted_file - full suite green (67 passed)
  (e) img.set_scene(scene) moved inside the per-scene try block (was outside it originally - a scene-switch
      failure would otherwise propagate past the whole file, discarding every already-successful scene)
- qc_figures.py: both silent except blocks now logger.warning(..., exc_info=True); both coor= typos fixed
  (there were 2 occurrences, not just the 1 originally found by the audit)
- try/except only at run_pipeline orchestration layer - held; _process_scene/_process_file's helpers remain
  exception-transparent
- test_cli.py updated to mock configure_logging (see item 2) - suite no longer leaks output/logs/*.log
  into the repo root on every run

DESIGNED 2026-07-30 (brainstormed with Claude, design agreed in chat - not yet implemented), closes both
open items below at once:

- catch+skip (operational) vs fail-fast (programmer/invariant) exception taxonomy - RESOLVED. New module
  src/spot_detector/exceptions.py, single-root hierarchy (standard Python ecosystem convention, cf.
  requests.exceptions/django.core.exceptions):
    SpotDetectorError(Exception)               - root for all exceptions this package defines intentionally
      FatalPipelineError(SpotDetectorError)     - "this will recur identically on every item, never
                                                   catch-and-continue, let it abort the run"
        DimensionMismatchError(FatalPipelineError, ValueError)  - the one concrete case today: do_3d
                                                   disagrees with actual data, raised by assign_spots_to_mask
                                                   instead of a bare ValueError. Multiple-inherits ValueError
                                                   too, so it's still semantically "a bad argument value" AND
                                                   existing `except ValueError`-based code/tests (test_
                                                   assigment.py's test_dimension_mismatch_raises) keep working
                                                   unchanged.
  Deliberately a small hierarchy, not just one narrow exception class for the single known case - chosen to
  leave room for future fatal-error cases without redesigning the taxonomy again (tradeoff: more structure
  than strictly needed for one case today, accepted deliberately, not an oversight).
  Wiring: both catch sites in run_pipeline.py (the scene loop in _process_file, the file loop in
  run_pipeline) get `except FatalPipelineError: raise` added BEFORE their existing `except Exception`, so a
  fatal error is never caught-and-logged-and-continued at either level. Once uncaught, it propagates all the
  way out of run_pipeline() and terminates the process via Python's normal unhandled-exception behavior
  (non-zero exit, full traceback) - decided this aborts the WHOLE RUN immediately (not just the current
  file), since the cause (do_3d, a run-wide config setting) would recur identically on every file/scene
  anyway; no custom process-termination machinery needed, letting it propagate uncaught already does this.

- make_qc_figure() discarding a valid scene_df on failure - RESOLVED, two-layer fix:
    1. Panel-level (extends _panel_ecdf's existing "No Spots Detected" placeholder pattern to failures
       generally): each of the 6 _panel_*(...) calls in make_qc_figure gets its own try/except - on failure,
       log a warning (exc_info=True) and draw a placeholder (e.g. ax.text(..., "Panel unavailable", ...)) on
       that panel's Axes instead of raising, then continue to the next panel.
    2. Figure-level (outer safety net in _process_scene): the whole make_qc_figure(...) call site gets wrapped
       - catches anything panel-level wrapping didn't (e.g. plt.savefig hitting a full disk), logs a warning,
       does NOT re-raise. scene_df (already computed above) still returns and gets aggregated normally.
       Deliberately NOT recorded in the failures list/CSV - that list means "this scene's DATA is missing";
       here the data is fine, only the picture is degraded/missing, so a logger.warning is the right level of
       visibility, not a failure record.
    Both layers get `except FatalPipelineError: raise` before their catch-all too, for consistency with the
    fail-fast rule above, even though a plotting function raising that type would be unusual.

- incidental bug found while scoping this (adjacent code, fix bundled in): _process_file's per-scene failure
  dict uses the key "Error Type" (space) while run_pipeline's per-file one uses "Error_Type" (underscore) -
  untested inconsistency, would produce two separate columns with gaps if scene- and file-level failures
  ever landed in the same run's failures CSV together.

DONE 2026-07-30: the exception-taxonomy half is fully implemented and tested. src/spot_detector/exceptions.py
added (SpotDetectorError -> FatalPipelineError -> DimensionMismatchError, single inheritance - NOT also
ValueError; decided during implementation to update the one affected test instead of keeping a dual-inheritance
backward-compat shim, since nothing else in the codebase caught bare ValueError around assign_spots_to_mask
anyway). assign_spots_to_mask raises DimensionMismatchError (with expected_ndim/actual_ndim attributes via a
custom __init__); its docstring updated to match. Both run_pipeline.py catch sites (_process_file's scene
loop, run_pipeline's file loop) have `except FatalPipelineError: raise` before their `except Exception`. The
Error_Type key fix (was "Error Type" with a space in _process_file) is in. Tests added: test_assigment.py's
test_dimension_mismatch_raises now checks DimensionMismatchError; test_run_pipeline.py gained
TestProcessFile.test_fatal_error_propagates_and_aborts_file and TestRunPipeline.test_fatal_error_propagates_
and_aborts_run, both proving a FatalPipelineError raised deep in the call chain actually escapes uncaught
(pytest.raises) rather than being caught-and-logged like an ordinary exception, and (the run-level one) that
the loop stops immediately - call_count == 2, not 3, meaning the item after the fatal one is never attempted,
and no run-level CSVs get written. 115 tests total (up from 113).

DONE 2026-08-25: the QC-figure-resilience half (qc_figures.py's panel/figure-level wrapping so a rendering
failure doesn't discard an already-computed scene_df - see the DESIGNED 2026-07-30 section above for the full
two-layer design) is now implemented. User wrote it (learning-mode - Claude reviewed only, did not write
code), Claude caught two rounds of real issues before it was correct:
  - initial pass: both qc_figures.py (_panel_segemntation) and run_pipeline.py's _process_scene figure-level
    wrapper were missing `except FatalPipelineError: raise` before their catch-all - qc_figures.py didn't
    even import FatalPipelineError yet. Fixed, then replicated correctly across all remaining 5 panels
    (_panel_spot_detection, _panel_ecdf, _panel_spotmap, _panel_flow, _panel_z_distribution) - all now
    consistently pass FatalPipelineError through before their `except Exception` catch-all, same
    "Failed to render" ax.text() placeholder pattern.
  - second pass, specific to _panel_z_distribution (the only panel needing real new try/except structure
    rather than a one-line fix, since it has two independent code paths - 3D stacked histogram vs 2D
    NND/KDE - each wrapped separately): (a) the n_obj/ax.legend() call for the 3D branch originally sat
    AFTER the try/except, so a failed ax.hist() call would still hit ax.legend() with no labeled artists on
    the axes (harmless but a stray matplotlib UserWarning + possible empty legend box drawn over the
    "Failed to render" placeholder) - fixed by moving both inside the try, right after ax.hist(), so they're
    skipped on failure same as everything else; (b) the pre-existing inner KDE-only try/except (finer-
    grained resilience predating this work - a failed KDE fit drops just the smoothed curve, keeps the
    histogram) was itself missing `except FatalPipelineError: raise` before its own catch-all, added for
    consistency with every other catch-all in the file.
  All 6 panels + make_qc_figure + _process_scene now correctly wired. _flow_to_rgb needed no changes -
  it's a pure data-transform helper called INSIDE _panel_flow's try, not a _panel_* function itself, and
  already documents its one raise (ValueError) correctly.
  DONE 2026-08-26: the docstring FOLLOW-UP is closed - all 8 functions that can raise/propagate
  FatalPipelineError (the 6 _panel_* functions, make_qc_figure, _process_scene) now document it with a
  Raises: section. User wrote it (learning-mode - Claude reviewed only, did not write code); Claude's review
  caught one gap: make_qc_figure itself was initially missed, since it calls all 6 panels unwrapped (no
  try/except in its own body - _process_scene is what wraps the make_qc_figure(...) call) - so a
  FatalPipelineError from any panel passes straight through make_qc_figure uncaught, same as the panels
  themselves, and that needed its own Raises: section too. Fixed, all 8 confirmed correct on review.
  test_qc_figures.py coverage (item 4) is still separately open - see below.

NEW GAP FOUND 2026-08-25 (not part of the original DESIGNED 2026-07-30 scope, symmetric bug one level up):
make_scene_summary_figure and make_run_summary_figure have the SAME "plotting failure discards already-
persisted data" problem make_qc_figure had, and neither is fixed yet:
  - make_scene_summary_figure - called unwrapped in _process_file (run_pipeline.py), AFTER combined_df is
    already written to its per-file CSV. A failure here propagates up to run_pipeline's per-file try/except,
    which DOES catch it - but mislabels it as a whole-file failure ("Processing file {filepath.name} failed",
    a Source_File-level record in the failures CSV) even though the actual per-scene data was fine and
    already safely on disk. combined_df's return value is discarded, so that file's rows silently drop out
    of the run-level rollup (_run_objects_{mode}.csv) and the run summary figure.
  - make_run_summary_figure - called unwrapped at the very end of run_pipeline() itself, AFTER run_df is
    already concatenated and written to _run_objects_{mode}.csv. NOTHING catches this - confirmed cli.py's
    main() calls run_pipeline(config=config) with zero try/except. A failure here crashes the whole process
    with an unhandled traceback, even though every CSV the run produced is already correctly on disk. Worse
    than make_qc_figure's original bug (which at least got caught, just mishandled) - this one isn't caught
    at all.
DONE 2026-08-26 (design agreed in chat, then implemented by user - learning-mode, Claude reviewed only):
  - fix is call-site-only wrapping, same shape _process_scene already uses around its make_qc_figure call
    (run_pipeline.py lines ~266-288): wrap each of the two call sites in try/except FatalPipelineError: raise
    / except Exception: logger.warning(..., exc_info=True), then fall through and return the already-computed
    DataFrame regardless (combined_df / run_df). NOT recorded in the failures list/CSV, same reasoning as
    make_qc_figure's fix - that list means "this row's DATA is missing", but here the data is fine, only the
    picture is degraded/missing.
  - explicitly decided AGAINST panel-level wrapping (i.e. NOT extending each function's 4 inline panels A-D
    with their own try/except + placeholder, unlike make_qc_figure's 6 separate _panel_* helpers): these
    panels are simpler/more robust (swarmplot/boxplot+stripplot/histogram/scatter, no per-panel image
    decoding or KDE fitting like the QC panels have) and doing it properly would need extracting each into
    its own function first - same restructuring qc_figures.py already went through for make_qc_figure,
    judged not worth it here. Revisit only if a real per-panel failure mode shows up in practice (e.g. Panel
    C's CV calc, x.std()/x.mean(), NaN or div-by-zero on a single-row group).
  - call site 1 (make_scene_summary_figure, in _process_file): wrapping locally at the call site is what
    fixes BOTH problems at once - the exception no longer escapes to run_pipeline's file-level catch (so it's
    no longer mislabeled as a Source_File-level failure), and combined_df still returns since it's computed
    above the try block.
  - call site 2 (make_run_summary_figure, in run_pipeline() itself): same pattern: wrap the call, return
    run_df regardless.
  IMPLEMENTED as designed, verified by reading the diff: both call sites (run_pipeline.py lines 114-127 for
  make_run_summary_figure, lines 204-217 for make_scene_summary_figure) wrapped exactly per the design above,
  no panel-level changes, neither failure recorded in `failures`, both DataFrames still returned regardless.
  Bonus (not scoped, but correct and welcome): run_pipeline() and _process_file() also picked up Raises:
  FatalPipelineError docstring sections of their own (previously only _process_scene had one) - accurate,
  since both do have `except FatalPipelineError: raise` in their loops, so it does propagate out of them too.
  Full suite re-run after the change: 115/115 passing.
  Item 1 (error handling & robustness) is now fully closed - both the original QC-figure resilience design
  and this symmetric summary-figure gap are implemented and verified.

2. logging & observability   <- print() -> logger conversion done, all known items closed
- DONE (confirmed still true 2026-07-30): configure_logging() is called from cli.py's main() before
  run_pipeline (now configure_logging(log_dir=Path(config.paths.out_dir) / "logs"), updated to attribute
  access by the item 5 config migration) - real log file + console INFO+ output on a real run.
- mode.upper() in run_pipeline.py's output filenames (_run_objects_2D.csv etc.) is INTENTIONAL, not a bug -
  confirmed still true 2026-07-30: all assertions in test_run_pipeline.py already match (uppercase 2D/3D
  throughout), no stale lowercase assertions remain.
- DONE 2026-07-27: test_cli.py now mocks configure_logging - confirmed by running the suite twice and
  checking `git status`: before the fix, every run left an untracked output/logs/run_*.log in the repo
  root (real handlers attached to the root logger via the unmocked call); after, nothing leaks
- record which config produced which output CSV - done (cli.py logs resolved config on load, once wired up)

DONE 2026-09-02: investigated and fixed a real run's log file being 99.2% unreadable noise (60,768 of
61,237 lines in one run) plus a garbled/duplicated-looking terminal progress bar. Both root-caused with
hard evidence (grep'd line counts/timestamps, read tqdm's actual installed source), not guessed at:
  - matplotlib.font_manager DEBUG spam (the 60,768 lines): configure_logging() sets root.setLevel(DEBUG)
    with file_handler also at DEBUG (cli.py), so ANY library using stdlib `logging` - not just
    spot_detector.* - gets captured. matplotlib's font_manager logs one DEBUG line per installed font
    it scores, the FIRST time each distinct font spec (title size, legend size, bold suptitle, etc.) is
    requested - confirmed via grep this is a near-total one-time cost (~15s window at the very start of
    the run, one tiny follow-up burst later for a new bold/size-13 spec). A commented-out
    `# logging.getLogger("matplotlib").setLevel(logging.WARNING)` was already sitting in cli.py,
    unused - someone had already spotted this fix and never enabled it.
  - Related: a one-time "Skipping backend candidate 'macosx'... ImportError" traceback - matplotlib
    auto-probing for a GUI backend on first pyplot use, since nothing ever calls matplotlib.use(...).
    Harmless (falls through to qtagg) but pure overhead for a pipeline that only ever calls
    savefig()+close(), never show() - also a real risk on a headless cluster node with no working GUI
    backend at all (relevant to item 3 below).
  FIX applied (learning-mode, Claude described, user applied, both verified empirically after):
    (a) cli.py: uncommented+enabled the matplotlib silence line, added the same for `fsspec` (27
        `open file` DEBUG lines, smaller but same category) - `logging.getLogger("matplotlib").
        setLevel(logging.WARNING)` / `logging.getLogger("fsspec").setLevel(logging.WARNING)`.
    (b) qc_figures.py: added `import matplotlib; matplotlib.use("Agg")` before `import matplotlib.
        pyplot as plt` - forces the headless backend explicitly, eliminates the GUI-probe traceback
        entirely (verified: `matplotlib.get_backend()` returns "Agg", zero probe output on import).
  Verified on a full real 3-file/21-scene rerun: log file dropped from 61,237 lines (single file) to
  381 lines (all three files) - a 99%+ reduction, confirmed by direct before/after comparison.
  SEPARATE bug found and fixed in the same investigation - tqdm progress bar duplication/garbling,
  reported as "multiplied at the start, fine by the end": root-caused by reading tqdm.contrib.logging's
  actual installed source (not guessed). `logging_redirect_tqdm()` finds the real console StreamHandler
  (correctly set to INFO) and substitutes a new `_TqdmLoggingHandler` for the duration of its `with`
  block - it copies the original handler's formatter and stream, but NEVER copies its level. The new
  handler defaults to NOTSET (accepts everything), so every DEBUG-level message (Scene N/M,
  Segmenting..., Detecting spots..., [QC] Saved summary) that should be console-filtered suddenly prints
  to the terminal for as long as the tqdm context is active - this is an upstream tqdm quirk/gap, not a
  spot_detector bug, but it manifests here as unwanted terminal verbosity. This directly explains the
  reported "duplication": each of those 6 now-unfiltered messages per scene forces a bar redraw, and
  scene 0's bar has no rate/ETA data yet (first iteration), so all 6 redraws print the identical
  `0%|  | 0/6 [00:0X<?, ?it/s]` text - looks exactly like a stuck/duplicated line. From scene 1 onward
  the same redraw-per-log-line mechanism continues (nothing structurally changes) but now shows real,
  varying percentages/ETAs each time, so it reads as a normally-updating bar instead - "bad at the
  start, fine by the end" is this artifact, not a warm-up effect.
  FIX applied (run_pipeline.py, learning-mode, Claude described, user applied): immediately after
  entering `with logging_redirect_tqdm():` in _process_file's scene loop, re-assert the console
  handler's level explicitly, since logging_redirect_tqdm won't do it:
    for h in logging.root.handlers:
        if isinstance(h, logging.StreamHandler) and not isinstance(h, logging.FileHandler):
            h.setLevel(logging.INFO)
  VERIFIED via a real before/after terminal capture comparison (captured with `2>&1` - console_handler
  defaults to stderr, and the FIRST capture attempt used stdout-only redirection and came back empty,
  a separate minor gotcha worth remembering for next time): scene 0's console redraws dropped from 6
  down to 2 (just the two genuinely-INFO messages, Found N object(s)/Detected N spot(s)), matching
  pre-investigation console verbosity exactly, while the LOG FILE (confirmed by reading it directly)
  still has full DEBUG detail (Scene N/M, Segmenting..., Detecting spots..., Saved summary all present) -
  the fix is surgical, console-only, no loss of file-logged detail. Full suite re-run clean: 138/138.
  One cosmetic thing NOT a bug, noted so it isn't re-investigated later: a one-time PyTorch
  `UserWarning: Sparse invariant checks are implicitly disabled...` from inside cellpose/dynamics.py can
  visually collide with the tqdm bar in a captured transcript - this comes from Python's `warnings`
  module, which writes straight to stderr and bypasses `logging` entirely, so it's unaffected by any of
  the above and not something to chase further (fires once per process, harmless).

3. cluster / parallelization readiness
- DONE (confirmed still true 2026-07-30): segmentation.use_gpu is read - utils.py's _load_cellpose passes
  gpu=config.segmentation.use_gpu to CellposeModel (fixed 2026-07-27, updated to attribute access by the
  item 5 config migration). No longer dead/hardcoded.
- no checkpointing/resume for interrupted runs - DEFERRED to the Snakemake migration (2026-07-27 discussion,
  see LEARNING_NOTES.md), not something to hand-build in run_pipeline.py now. Reasoning:
    - overnight-run crash resilience splits into two failure classes: catchable Python exceptions (CUDA
      OOM, corrupt file, etc. - already handled per-file/per-scene, see item 1) vs. the process getting
      SIGKILLed outright (OS OOM killer, preemption, power loss) - the latter can NEVER be caught or logged
      from inside the process, so the only real defense is making sure completed work is already durable on
      disk before the kill, not smarter exception handling. Per-file CSVs already satisfy this; the
      run-level rollup (_run_objects_{MODE}.csv) currently only gets written once at the very end and could
      instead be reconstructed by globbing tab_dir for already-written per-file CSVs - cheap, still open.
    - "redo with a new model/params" vs. "resume a killed run with the same config" need to be told apart,
      or a naive skip-if-exists resume would silently reuse a DIFFERENT config's stale output and blend two
      runs' results under one filename with no visible error. Planned workflow: symlink raw data + fresh
      output folder per analysis variant (matches the old repo-per-run notes at the top of this file) makes
      "redo" safe by construction (nothing to collide with); "resume in place" would need either a config
      fingerprint check or an explicit refuse-to-overwrite-unless-forced guard - not built.
    - Snakemake's DAG + rule params already solve exactly this (only rebuild targets whose inputs/params
      changed) - building a hand-rolled version now risks a weaker, unsafe duplicate of what the planned
      migration (item 3 of the original pre-audit list, `workflow/`) gives for free. Revisit there instead.
- separately (not deferred): after catching a CUDA OOM, call torch.cuda.empty_cache() before continuing to
  the next scene/file - the caching allocator can leave fragmented/reserved memory behind after an OOM, so
  without this the *next* item can fail too even though it would otherwise fit
- fully sequential file/scene loops, no parallel scheduling hooks for snakemake
- GPU memory measured empirically 2026-07-27 (RTX 4060 laptop, 7.8GB total, real .nd2 scene from
  data/None_0.nd2, both configured checkpoints: cpsam_pseudo3d_4x_20260506 + bag_spot_model).
  Numbers are torch.cuda's *reserved* memory (what the OS sees as unavailable to a second
  process), not *allocated* (live tensors) - reserved is the number that matters for deciding
  how many concurrent worker processes fit on one GPU, since the caching allocator doesn't
  release it until the process exits.
    - both models loaded, idle: ~1.8GB reserved
    - peak during one scene (segment + detect), 3D mode: ~2.7GB reserved
    - peak during one scene, 2D mode: ~3.9GB reserved (but see bug below - this ran the
      wrong spotiflow model)
  -> tentative verdict: process-per-GPU-slice parallelization looks feasible on shared cluster
     GPUs with >=16-24GB (2-4 concurrent workers with headroom); tight on an 8GB card (1 worker
     safely, 2 only with no other tenant and some risk). NOT yet a green light - the test scene
     had only 1 object / 18-49 spots, and Cellpose's flow-field postprocessing memory scales with
     instance count, not just image size, so a busy real FOV (dozens-hundreds of BAGs) needs the
     same measurement before sizing cluster GPU requests. Re-run probe against a representative
     high-density scene before committing to a parallel worker count.
  -> BUG surfaced during this measurement: config.yml has mode.do_3d: false, but the configured
     spotiflow_models_path (bag_spot_model) is a 3D-only model. ModelBundle's documented fallback
     chain (utils.py) catches the mismatch and silently substitutes the generic pretrained
     synth_complex model instead of raising - so a real 2D run today silently skips the user's
     trained spot model. Either do_3d should be true, or spotiflow_models_path needs a 2D-trained
     model - needs a decision, not just a fallback.
  -> concurrency stress test 2026-07-27: ran 2 concurrent 2D-mode workers for real on the 8GB
     card (worst case ~3.9GB/worker individually). Both completed correctly, peak COMBINED memory
     via nvidia-smi (not torch's self-reported stats) = ~6.96GB - but worker 2's log shows a
     transient CUDA OOM warning mid-run (~80MB free at that instant) that the allocator recovered
     from. Conclusion: 8GB is maxed out / not safe for 2 workers in production. On 12GB that
     leaves ~5.3GB headroom over the observed peak, so 2 workers should fit reliably there - but
     re-run this same concurrency test against a dense real scene (many objects/spots, not the
     1-object/49-spot calibration scene used so far) before finalizing worker count for the
     cluster request.

4. testing coverage   <- reassessed 2026-07-15, mostly in good shape
- only real gap: qc_figures.py (691 lines, largest module) has zero tests, no test_qc_figures.py exists.
  SCOPED 2026-07-30 alongside item 1 (brainstormed together since item 1's QC resilience fix touches this
  exact module): new test_qc_figures.py should cover, in full - each of the 6 _panel_* functions (correct
  plotting behavior + edge cases like has_spots=False), SpotData/ImageData's __post_init__ unit-conversion
  logic, the three figure builders (make_qc_figure/make_scene_summary_figure/make_run_summary_figure)
  end-to-end, PLUS the new panel-level/figure-level resilience behavior from item 1's design (a failing
  panel gets a placeholder and doesn't crash the figure; a failing make_qc_figure call doesn't prevent
  scene_df from returning). Explicitly chosen as full coverage in this pass, not just the new resilience
  behavior, given the module's size and that it's never been touched - a bigger effort than item 1's own
  implementation, worth treating as its own follow-on pass once item 1 lands.
  IN PROGRESS 2026-09-01: test_qc_figures.py under construction, working through the scope above
  section by section (skeleton for every section - SpotData/ImageData, all 6 panels, all 3 figure
  builders - already laid out as empty classes). SpotData/ImageData section DONE first (11 tests,
  learning-mode - Claude reviewed/explained only, user wrote every test), all passing, 126 tests total
  suite-wide. Two real bugs in qc_figures.py found and fixed while writing these tests (not just test
  coverage - actual source fixes):
    - SpotData.__post_init__: x/y/z/x_um/y_um/z_um are field(init=False) with no default, and were only
      ever assigned inside `if self.has_spots:` - so when has_spots=False, those six attributes didn't
      exist at all (not None), contradicting the docstring's `np.ndarray | None` contract. Confirmed via
      repr() on such an instance raising AttributeError (dataclass-generated __repr__ touches every
      field). Fixed: added an `else:` branch in __post_init__ assigning all six to None. No caller
      changes needed - every existing caller already checked `has_spots and x is not None` with
      short-circuit `and`, so this was latent, not yet triggered in production.
    - Same class of bug, second instance: even when has_spots=True, if is_3d=False, z_um was never
      assigned (only set inside the is_3d branch). Fixed: added `self.z_um = None` in the is_3d=False
      branch (None, not zeros, since z_um isn't meaningful in 2D mode - _panel_spotmap only reads it
      inside its own `if spots.is_3d:` check).
  Testing/technique notes worth remembering for the remaining sections:
    - pytest.approx() rejects nested list-of-lists (e.g. a 2D array's .tolist() result) - use it
      directly on the numpy array instead of calling .tolist() first; reserve .tolist() for exact
      integer-array equality (labels, pixel indices) where plain == is correct and stricter.
    - np.round uses round-half-to-even ("banker's rounding"), not round-half-up - worth an explicit
      test (SpotData.test_rounding_uses_round_half_to_even) so a future refactor to plain round()
      doesn't silently change behavior at exactly .5 boundaries.
  Panel-testing conventions established (apply to all remaining panel classes):
    - Axes is mocked (`ax` fixture in the file's Fixtures section, returns mocker.MagicMock()), not a
      real matplotlib Axes - fast, and directly verifies "was this called with the right data" via
      call_args/call_args_list, matching how test_detection.py already mocks the Spotiflow model.
    - images/spots inputs are SimpleNamespace stand-ins, not real ImageData/SpotData instances - only
      set the specific attributes the panel under test actually reads, decoupling panel tests from the
      dataclasses' own correctness (already covered separately by TestImageData/TestSpotData).
    - a `# type: ignore[arg-type]` comment is needed on every panel call site, since SimpleNamespace
      isn't nominally an ImageData/SpotData - cosmetic only, this repo has no mypy/pyright gate (checked
      pyproject.toml + .pre-commit-config.yaml, only ruff runs), so it never affects CI or commits.
    - every panel test class covers: happy-path branches, `except FatalPipelineError: raise` propagates
      uncaught (and any code after the try/except block does NOT run - proven via an
      assert_not_called() on that trailing code), and a generic Exception falls back to the "Failed to
      render" placeholder AND still runs the code after the try/except (contrast with the FatalPipelineError
      case proves the two except branches behave differently, not just that neither crashes the test).
  TestPanelSegemntation DONE (6 tests) and TestPanelSpotDetection DONE (6 tests) - both learning-mode,
  Claude reviewed/explained only. 138 tests total suite-wide as of 2026-09-01.

  BUG FOUND 2026-09-01, NOT YET FIXED (surfaced while scoping TestFlowToRgb/TestPanelFlow - work paused
  here, resume next session): _flow_to_rgb's channel-index comments/docstring AND its 3D-branch code
  disagree with Spotiflow's real flow-field layout. Verified against ground truth in Spotiflow's own
  installed source (not guessed) - .venv/lib/python3.12/site-packages/spotiflow/utils/peaks.py,
  flow_to_vector_2d/flow_to_vector_3d, whose docstrings state the real channel order explicitly:
    flow_to_vector_2d: "3d flow (z',y',x')" -> `z, y, x = flow.transpose(2, 0, 1)` - real 2D layout is
      [stereographic, y, x] (stereo FIRST, not last).
    flow_to_vector_3d: "4d flow (w',z',y',x')" -> `w, z, y, x = flow.transpose(3, 0, 1, 2)` - real 3D
      layout is [stereographic, z, y, x] (stereo FIRST, not last).
  qc_figures.py's _flow_to_rgb docstring (and the inline comments above each branch) claim the OPPOSITE
  order - [fy, fx, stereographic] for 2D, [fz, fy, fx, stereographic] for 3D (stereo LAST). Comparing
  against the actual code in each branch:
    - 2D branch (`fy = flow_data[..., 1]`, `fx = flow_data[..., 2]`): CODE IS CORRECT (real layout
      [stereo, y, x] means index1=y, index2=x, exactly what the code reads) - only the
      docstring/comment is wrong, purely cosmetic, no functional bug.
    - 3D branch (`fy_vol = flow_data[..., 1]`, `fx_vol = flow_data[..., 2]`): CODE IS ACTUALLY WRONG.
      Real layout [stereo, z, y, x] means index1=z (not y!), index2=y (not x!), and the true x-component
      lives at index3, which the code never reads at all. The 3D flow panel is currently rendering the
      Z and Y flow components mislabeled as Y and X, and silently ignores the real X-component entirely.
      This is a real rendering bug (wrong data shown), not just a docstring mismatch.
  FIXED 2026-09-02 AND EMPIRICALLY VERIFIED against real data (not just source-reading): 3D branch
  indices shifted (`fy_vol = flow_data[..., 2]`, `fx_vol = flow_data[..., 3]`), both docstrings/comments
  corrected to state the real order ([stereographic, y, x] for 2D, [stereographic, z, y, x] for 3D,
  stereo first not last in both) - learning-mode, Claude described the exact edit, user applied it.
  Verification method (src/flow_details.py, extended for this purpose - see below): ran real detection
  on data/allON_HP.nd2 scene 1 in both modes, then compared _flow_to_rgb's (now-fixed) fy/fx channel
  choice against Spotiflow's own decoded ground truth, `details.subpix` (= flow_component * s, where
  s = sigma/(1+z'+eps) is always positive, so a correct channel choice must SIGN-agree with subpix at
  nearly every pixel - a wrong choice would show ~50% coin-flip agreement instead). Result: 100% sign
  agreement for both y and x, in both 2D (was already correct) and 3D (previously broken, now fixed)
  modes - the strongest available confirmation short of a full stereographic re-derivation.
  Real shapes observed along the way (useful reference, not part of the bug itself): 2D details.flow
  (2048, 1600, 3), details.subpix (2048, 1600, 2); 3D details.flow (15, 1024, 800, 4), details.subpix
  (15, 1024, 800, 3) - note the 3D run's spatial dims come back downsampled vs the 2D run on the same
  image (Spotiflow's own internal downsample_factors for that model config, unrelated to this bug).
  NEXT SESSION: write TestFlowToRgb (real arrays, no mocking, per the earlier design decision to
  separate it from TestPanelFlow) verifying the corrected 2D/3D channel selection, THEN TestPanelFlow
  itself (mocked Axes + mocked _flow_to_rgb for the success path, per the same earlier decision)
  covering its 5 branches (None flow_details / heatmap fallback 2D+3D / non-ndarray flow / success /
  render-failure-in-_flow_to_rgb) plus the usual FatalPipelineError-propagates test.

  test_qc_figures.py state as of 2026-09-02 (for picking this back up next session): SpotData (4 tests)
  and ImageData (7 tests) sections fully done. TestPanelSegemntation (6 tests) and TestPanelSpotDetection
  (6 tests) fully done. The _flow_to_rgb bug fix above is now DONE and verified, so TestPanelFlow is no
  longer blocked. TestPanelFlow, TestPanelZDistribution, TestPanelECFD, TestPanelSpotmap, and a new
  (not yet created) TestFlowToRgb class are all still to write. TestMakeQCFigure,
  TestMakeRunSummaryFigure, TestMakeSceneSummaryFigure (the 3 figure-builder classes) also still empty
  `pass` stubs, not yet scoped/discussed in detail beyond the original item-4 scoping paragraph above.
  23 tests in test_qc_figures.py so far, 138 total suite-wide (unchanged by this session's bug fix -
  it touched src/spot_detector/qc_figures.py and src/flow_details.py, a standalone scratch/diagnostic
  script outside the test suite, not tests/test_qc_figures.py itself).

  DONE 2026-09-02 (cont'd): TestFlowToRGB written (learning-mode - Claude advised on synthetic-data
  construction + assertion style, user wrote every test). 8 tests:
    - 2D output contract - shape (Y,X,3) / dtype / [0,1] bounds. Confirmed empirically that
      mcolors.hsv_to_rgb preserves the float32 the function feeds it (out.dtype == np.float32 passes),
      so _flow_to_rgb's docstring dtype claim holds - was an open question from the earlier scoping.
    - 3D output collapses the Z axis (random (Z,Y,X,4) -> (Y,X,3)).
    - 3D best-z selection - one "loud" z-slice (fy=3 at idx2, fx=4 at idx3), all others zero, asserted
      to produce the identical RGB as its hand-built 2D equivalent ((Y,X,3) with fy=3 at idx1, fx=4 at
      idx2). The single equality simultaneously exercises best_z selection AND the per-branch channel
      index maps the 2026-09-02 fix corrected.
    - 3D ignores stereographic + z channels - REGRESSION LOCK for the 2026-09-02 fix: garbage (100.0)
      in indices 0 and 1, zeros in 2 and 3, output must be all-white. The OLD buggy code read index 1
      as fy -> magnitude 100 -> not white -> this test would have failed on it.
    - zero-flow-is-white (2D) - simplest predictable-output case.
    - bad-shape ValueError - parametrized x3: ndim 2, ndim-3 last-axis<3, ndim-4 last-axis<3.
  All pass. test_qc_figures.py now 31 real tests (SpotData 4 + ImageData 7 + PanelSegemntation 6 +
  PanelSpotDetection 6 + FlowToRGB 8) plus 8 not-yet-implemented TestPanelFlow `pass` stubs the user
  scaffolded as a plan -> 39 collected in that file, 146 real / 154 collected suite-wide.
  Technique notes for the remaining classes:
    - pytest.approx ordering operators (>=, <=) do NOT work with a numpy scalar on the left:
      `out.min() >= pytest.approx(0.0)` raises TypeError ("'>=' not supported between instances of
      'numpy.float32' and 'ApproxScalar'"). approx is fundamentally an equality tool; its </<=/>/>=
      support is a fragile bolt-on that only works with plain Python floats. For bounds checks use
      plain `out.min() >= 0.0` / `out.max() <= 1.0` (verified: _flow_to_rgb output is exactly [0.0,
      1.0], no float overshoot at the clip boundary, so no epsilon needed). Whole-array approximate
      equality still uses `actual == pytest.approx(expected)` - ApproxNumpy does the elementwise
      compare + reduces to one bool, matching the existing line ~133 pattern.
    - the "zero flow -> pure white" trick: magnitude 0 forces saturation 0, and HSV with zero
      saturation is (V,V,V) regardless of hue, so the output is trivially predictable without
      reproducing the HLS->RGB math. Used for both the zero-flow and the channels-ignored tests.
    - np.testing (assert_allclose etc.) deliberately NOT introduced - the suite's established style is
      `== pytest.approx(...)` for arrays; consistency across the other 10 test files wins over
      np.testing's richer failure diagnostics.
  Optional polish deferred on TestFlowToRGB (not blockers): equiv_2d in test_3d_selects_highest_
  magnitude_z is np.zeros((6,7,4)) - functionally fine (2D branch only needs last-axis>=3) but real
  Spotiflow 2D flow is (Y,X,3); and that test has only one non-zero z-slice, so it proves "the
  non-zero slice wins over zero slices" rather than literally "highest of several" - judged adequate
  (argmin/first/last mutations all still caught).
  NEXT: TestPanelFlow (8 stubs already named in the file) - mocked `ax` + mocked _flow_to_rgb (patch
  at spot_detector.qc_figures._flow_to_rgb), covering: None flow_details / flow=None+heatmap-2D /
  flow=None+heatmap-3D-maxproj / flow=None+no-heatmap text / non-ndarray flow text / success path /
  render-failure "Flow Render Error" placeholder / FatalPipelineError propagates. Note _panel_flow
  calls set_title + axis("off") FIRST, then heatmap branches call set_title again to override - so
  the "trailing code after the try/except still runs" assertion the other panels use does NOT apply
  here, and the FatalPipelineError test just asserts propagation + `ax.imshow` not called.
  Then TestPanelZDistribution (user's note in file: "split in 2" - the 3D stacked-histogram path and
  the 2D NND/KDE path are independent branches), TestPanelECFD, TestPanelSpotmap, then the 3
  figure-builder classes.

  DONE 2026-09-03 (cont'd): TestPanelZDistribution written (learning-mode - Claude advised, user wrote
  every test). 11 tests, all pass, ruff clean. Structure: one top-level test for the shared
  `has_spots=False` early-out, then nested classes TestIs3dTrue / TestIs3dFalse for the two independent
  is_3d branches (first use of nested test classes in this repo - chosen so the one-top-level-class-per-
  panel mapping survives while the branches get separate scope + their own fixtures; the shared early-out
  test has a natural home in the outer class).
    - TestIs3dTrue has a `valid_3d` fixture returning a dict of complete, minimal inputs that actually
      REACH ax.hist (spots.z_um as a real array, spots.dz, images.spot_image with ndim==3,
      images.masks_2d) - needed because the 3D branch reads ~6 attributes in its loop before anything
      observable happens, and a missing one silently drops into `except Exception` -> "Failed to render",
      making a happy-path test pass for the wrong reason. Every happy-path test asserts
      ax.text.assert_not_called() as the guard against exactly that false positive.
    - stacked-histogram test pins the actual partitioning: `[a.tolist() for a in ax.hist.call_args[0][0]]
      == [[0.5],[1.0],[1.5]]` (spots bucketed into hist_data by spot_labels value), plus stacked=True,
      label list ["Background","Obj 1","Obj 2"], color[0]=="lightgray", bins from arange*dz.
    - legend gating: n_obj = len(np.unique(masks_2d)) - 1 (the -1 drops label 0); tests bracket the
      `n_obj < 10` threshold from both sides (masks_2d = arange(9) -> n_obj 8 -> legend shown;
      arange(12) -> n_obj 11 -> suppressed). Exact-boundary (n_obj == 10) test skipped as gold-plating.
    - TestIs3dFalse: the 2D branch reads ONLY spots.coordinates + spots.dx (no images, no spot_labels),
      so every test passes images=... / spot_labels=... (literal Ellipsis) and only builds a real spots.
    - degenerate gaussian_kde as a deterministic failure trigger: 2 spots -> NND [1,1] -> identical
      values -> gaussian_kde raises LinAlgError (singular covariance) -> exercises the 2D path's INNER
      try/except (curve dropped, histogram kept: ax.hist called, ax.plot NOT called). 4 spread-out spots
      -> NND [1,1,4,15] has variance -> KDE curve drawn (ax.plot called). No mocking of scipy needed.
    - 1 spot -> len(coordinates) < 2 -> "Insufficient Spots for NND" + ax.grid(False); test uses
      ax.grid.assert_any_call(False) since ax.grid is ALSO called with (True, ...) in the trailing code.
    - render-failure vs fatal, both branches: ax.hist.side_effect = RuntimeError -> "Failed to render"
      placeholder + trailing set_title STILL runs (assert_called_once); ax.hist.side_effect =
      FatalPipelineError -> pytest.raises + trailing set_title NOT called (assert_not_called) - the
      contrast assertion that distinguishes the two except branches.
  test_qc_figures.py now 50 real tests, 165 suite-wide. Technique notes: nested @pytest.fixture inside a
  nested test class works fine and is function-scoped (fresh per test, in-place mutation like
  `valid_3d["images"].masks_2d = ...` is safe); a degenerate numeric input that makes a real library call
  raise is cleaner than mocking that library.
  DONE 2026-09-03 (cont'd): TestPanelECFD written (learning-mode - Claude advised, user wrote every
  test). 5 tests, flat class (no nesting - _panel_ecdf is one linear flow, the has_spots split is plain
  if/else with no independent sub-algorithm). config + flow_details are SimpleNamespace stand-ins
  (config = SimpleNamespace(detection=SimpleNamespace(prob_thresh=0.4)) - only .detection.prob_thresh
  is read; make_config from conftest would work but builds real tmp files for pydantic validators, pure
  overhead here).
    - has_spots=False -> "No Spots Detected" + axis off, else-block never entered (ax.axvline not called).
    - splits inside (spot_labels > 0) vs background: spot_labels [0,1,0,2] + prob [0.2,0.8,0.4,0.9] ->
      inside subset sorted [0.8,0.9], background [0.2,0.4], both n=2. Asserts ax.step.call_count == 2 and
      inspects call_args_list[0] (inside, color #D4537E, ls "-") vs call_args_list[1] (background,
      #888780, "--"), plus ax.axvline positional arg == prob_thresh.
    - empty group skipped: all spot_labels > 0 -> background subset len 0 -> `continue` -> only 1
      ax.step call, ax.axvline still called. (prob list length MUST match spot_labels or prob_arr[mask]
      IndexErrors into the except -> a false-positive failure-path test.)
    - render failure vs fatal: ax.axvline.side_effect (axvline is the last statement in the try) =
      RuntimeError -> "Failed to render" + trailing set_title runs; = FatalPipelineError -> pytest.raises
      + trailing set_title NOT called.
  (fixed 2026-09-03: the fatal test's `ax.set_title.assert_not_called()` was indented INSIDE the
  `with pytest.raises(...)` block -> unreachable dead code; dedented to sit after the block.)
  test_qc_figures.py at 55 tests, 170 suite-wide.
  Two mock gotchas hit and fixed while writing this (see LEARNING_NOTES.md 2026-09-03 cont'd):
    - `mock.call_args` is the LAST call only; `call_args[0]`/`call_args[1]` are its (args, kwargs), NOT
      "first call"/"second call". For a method called in a loop, use `call_args_list[i]`.
    - `kwargs["label"] = "..."` (single =) inside a test is a silent dict assignment, asserts nothing -
      `mock.call_args` unpacks to a plain dict, so a typo'd `==` just mutates it.
  NEXT: TestPanelSpotmap. _panel_spotmap(ax, images, spots) - no has_spots early-out placeholder here
  (different shape). Reads images.segmentation_image.shape[-2:], images.masks_2d, spots.{has_spots, x,
  y, is_3d, z_um, dx}. Two independent guarded blocks: (1) `if images.masks_2d is not None` draws
  ax.contour of object boundaries (skipped if no levels > 0); (2) `if spots.has_spots and x/y not None`
  a try/except'd scatter, is_3d -> ax.scatter(c=z_um, cmap="turbo") + fig.colorbar (needs
  ax.get_figure() to return something), else -> plain blue ax.scatter. Trailing xlim/ylim (y inverted)
  /set_aspect("equal") always run. One except FatalPipelineError: raise / except Exception ->
  "Failed to render" around the scatter block only. Tests: contour drawn / contour skipped when
  masks_2d all background; 3D scatter + colorbar; 2D scatter; no-spots (contour still drawn, no
  scatter); render failure + fatal (scatter block). Then the 3 figure-builder classes.

  DONE 2026-09-03 (cont'd): TestPanelSpotmap written (learning-mode - Claude advised, user wrote every
  test). 7 tests, flat class, all pass, ruff+format clean.
    - images.segmentation_image is read UNCONDITIONALLY (first two lines: img_h/img_w from
      .shape[-2]/[-1]), so every test sets it even the contour-only / no-spots ones - np.zeros((4,4))
      suffices, only the shape is used.
    - contour block has NO try/except (only the scatter block does) - a failure there would propagate
      uncaught, asymmetry noted but not tested.
    - contour drawn: masks_2d [[0,1],[2,0]] -> levels = unique[unique>0] - 0.5 = [0.5,1.5] -> ax.contour
      called. contour skipped: masks_2d all-zero -> levels empty -> len(levels) > 0 False -> not called.
    - 3D scatter: ax.scatter(c=z_um, cmap="turbo"); ax.get_figure() on a MagicMock ax already returns a
      non-None mock, so the colorbar path runs with zero patching - assert
      ax.get_figure.return_value.colorbar.assert_called_once(). 2D scatter: color="#4FC3F7".
    - render failure vs fatal: ax.scatter.side_effect = RuntimeError -> "Failed to render" + trailing
      set_aspect("equal") STILL runs (assert_called_once); = FatalPipelineError -> pytest.raises +
      set_aspect NOT called (assert_not_called, AFTER the with block). set_aspect is this panel's
      "trailing code" marker (last unconditional line), same role set_title plays elsewhere.
    - the x/y-None defensive guard (has_spots True but x/y None -> scatter skipped) is covered by
      test_no_spots_still_draws_contours_no_scatter (has_spots=False + x/y None together).
  test_qc_figures.py now 62 tests, 177 suite-wide.

  NEXT (tomorrow, user's call): EITHER do items 7+8 first (split qc_figures.py -> qc_figures.py +
  qc_panels.py, and the fixture/readability pass on the test file) OR finish the last 3 figure-builder
  classes. User leans 7+8 first - the panel coverage that de-risks the split is now done, and the 3
  builder classes land just as easily after the split. Claude agrees with that ordering.

  FIGURE-BUILDER TEST PLAN (agreed 2026-09-03, user "i like the plan" - use this when the 3 classes get
  written, whenever that is):
  Principle: these are ORCHESTRATORS, not leaf drawers. The 6 _panel_* helpers are already tested
  directly, and _process_scene's figure-level resilience wrapper is tested in test_run_pipeline.py
  (item 1). So the builders need only: (a) a smoke test per mode - real render to a tmp_path PNG, assert
  the file exists + nothing raised; and (b) targeted tests for the builder's OWN logic (the mode branch,
  dispatch to collaborators, figure cleanup). Do NOT assert on plot contents / artist props / colors /
  positions - brittle, ~zero bug-catching value. Smoke tests are NOT worthless here: they catch import
  errors, matplotlib/seaborn API drift, wrong arg counts to panels, KeyError on a renamed DataFrame
  column, savefig failures. The DataFrame column contract (hard-coded "Spot_Count"/"Scene"/"Area_um2"
  etc.) is the single biggest real risk and a realistic-df smoke test is what catches an upstream rename.
  These will be the first tests in the file to actually render (all panel tests mock `ax`) - that's
  correct, and Agg is already forced in qc_figures.py so it stays offline/fast (~1s for all).
    TestMakeQCFigure:
      - test_smoke_2d / test_smoke_3d: real minimal arrays, out_path = tmp_path/"x.png",
        assert out_path.exists(), no raise. LET THE REAL PANELS RUN (valid-input knowledge already
        exists across the panel test classes) - makes this a real integration test for ~free.
      - test_dispatches_to_all_six_panels: mocker.patch the 6 _panel_*, assert each called once
        (optionally with ax == the right axes_flat[i]).
      - test_closes_figure: assert plt.close called - unclosed-figure leak is a real long-run OOM class
        (make_qc_figure runs once per scene, 21-scene runs exist).
    TestMakeSceneSummaryFigure:
      - test_smoke_2d / test_smoke_3d: small real DataFrame with the expected columns
        (Scene, Spot_Count, Area_um2 / Volume_um3), assert file written. Let seaborn actually run on the
        tiny df - only way the column-name check bites.
      - (optional) test_3d_selects_volume_columns: mock sns.boxplot, assert y="Volume_um3" when
        mode="3d".
    TestMakeRunSummaryFigure:
      - test_smoke_2d / test_smoke_3d: same, columns Condition, Spot_Density_per_um2 / _per_um3,
        Volume_um3 / Area_um2.
      - (optional) test_cv_panel: the groupby("Condition")["Spot_Count"].agg(x.std()/x.mean()) is real
        arithmetic - worth one test IF reachable; currently inline in the sns.scatterplot call, so only
        assertable if cv_df gets extracted first. Skip otherwise.
  ~8 core tests + ~2 optional, mostly smoke. Guards the two real risks: the mode branch and the
  DataFrame/argument column contract.

  DONE 2026-09-03: TestPanelFlow written (learning-mode - Claude advised, user wrote every test). 8
  tests, all pass, back to the mocked-`ax` panel style but also mocking _flow_to_rgb itself for the
  paths that reach it:
    - None flow_details -> "No Flow Data Provided" text placeholder, no imshow.
    - flow=None + 2D heatmap -> ax.imshow(heatmap, cmap="magma"), title overridden to "Probability
      Heatmap\n(flow unavailable - subpix disabled)". Asserts `args[0] is flow_details.heatmap`
      (identity - the 2D branch passes the heatmap straight through, ndim==2 so display = heatmap).
    - flow=None + 3D heatmap -> ax.imshow(heatmap.max(axis=0)). Input hand-built as a (3,2,2) array
      where each z-slice owns a different max pixel, so the assertion pins "element-wise max over Z"
      rather than "take the last slice" (a monotonic np.arange input could not tell those apart).
      Asserts shape collapsed to (2,2) AND `== pytest.approx(heatmap.max(axis=0))` (derived array ->
      value equality, not identity).
    - flow=None + no/None heatmap -> "Flow unavailable\n(model has compute_flow=False)" text.
    - flow is not an ndarray (a list) -> "Flow Render Error\nflow attribute is not a numpy array" text.
    - success path -> _flow_to_rgb mocked with return_value=mocker.sentinel.rgb (identity-comparable,
      sidesteps the ndarray-== ambiguity in assert_called_once_with); asserts _flow_to_rgb got the raw
      flow array (`call_args[0][0] is flow_details.flow` - unmodified pass-through), ax.imshow got the
      sentinel, no text, title NOT overridden (stays "Stereographic Flow").
    - render failure -> ax.imshow.side_effect = RuntimeError("boom") (drives the failure through the
      SECOND statement in the try, proving recovery from a failure in either line, not just in
      _flow_to_rgb); asserts "Flow Render Error\nboom" text. Expected string hard-coded, not rebuilt
      with an f-string, so a source-side message change fails the test loudly.
    - FatalPipelineError -> ax.imshow.side_effect = FatalPipelineError(...); pytest.raises + (the
      contrast assertion that makes this test distinct from the render-failure one)
      ax.text.assert_not_called() - proves `except FatalPipelineError: raise` runs INSTEAD of the
      placeholder branch. Note: _panel_flow has no code after its try/except, so unlike the other
      panels there is no "trailing code still runs" assertion to make.
  test_qc_figures.py now 39 real tests (SpotData 4 + ImageData 7 + PanelSegemntation 6 +
  PanelSpotDetection 6 + FlowToRGB 8 + PanelFlow 8), 154 total suite-wide (all real now - the 8
  PanelFlow `pass` stubs are implemented; the remaining empty classes contribute 0 collected tests).
  Technique notes for the remaining classes:
    - mocker.sentinel.<name> for a mock's return_value when the test only needs "was THIS object
      passed on" - compares by identity, so assert_called_once_with(sentinel) works where a real
      ndarray would raise on ==.
    - identity (`is`) vs value (`== pytest.approx`): assert `is` when the production code forwards the
      argument untouched (stronger - also catches an accidental copy/transpose); switch to
      pytest.approx the moment the code derives a new array (.max(), slicing, np.asarray).
    - when two tests exist to prove two branches behave DIFFERENTLY (render-failure placeholder vs
      FatalPipelineError re-raise), each needs an assertion the other fails - here
      ax.text.assert_not_called() in the fatal test. Without it both tests just say "didn't crash".
  KNOWN (user to fix before commit): line ~513 `assert ... == f"Flow Render Error\nboom"` has an
  f-string with no placeholders -> ruff F541. Drop the `f` prefix. (pre-commit ruff-check --fix would
  auto-strip it anyway, but cleaner to fix in place.)
  NEXT: TestPanelZDistribution - user has already left a "# split in 2" marker in the stub. _panel_z_
  distribution has two fully independent top-level branches gated on spots.is_3d: the 3D path draws a
  per-object stacked histogram of spots-per-z-plane (with its own inner legend-gating on n_obj < 10);
  the 2D path computes nearest-neighbour distances via scipy KDTree + an inner try/except'd
  gaussian_kde curve. Plus the shared `if not spots.has_spots` early-out ("No Spots Detected" +
  axis off) above both, and the 2D-path's own `len(coordinates) < 2` -> "Insufficient Spots for NND"
  sub-case. Each branch has its own except FatalPipelineError: raise / except Exception ->
  "Failed to render" wrapper, and the 2D path has a SECOND nested try/except around just the KDE fit
  (a failed fit drops the smoothed curve but keeps the histogram) - that inner one also has its own
  except FatalPipelineError: raise. So: has_spots=False, 3D happy path, 3D n_obj<10 legend vs
  n_obj>=10 no-legend, 3D render failure + fatal, 2D <2 spots, 2D happy path, 2D KDE-only failure
  (curve dropped, hist kept), 2D outer render failure + fatal.
- everything else has real coverage: test_run_pipeline.py (12, real PipelineConfig fixtures as of 2026-07-29
  - see item 5), test_model_bundle.py (14 as of 2026-07-30, up from 8 - flag-wins precedence coverage added,
  see item 5), test_object_measurement.py (11), test_detection.py (5), test_segmentation.py (7),
  test_assigment.py (5), test_config.py (44 as of 2026-07-28, up from 4 - full PipelineConfig schema
  coverage, see item 5), test_cli.py (2), test_utils.py (1 parametrized, 13 cases). 126 tests total as of
  2026-09-01 (115 as of 2026-07-30's exception-taxonomy work, item 1 above, +11 test_qc_figures.py
  SpotData/ImageData coverage - see IN PROGRESS note above; the "113" figure in this paragraph's opening
  line is stale, predates the 2026-07-30 FatalPipelineError test additions).
- DONE 2026-07-27: scene/file isolation tests added (TestProcessFile.test_handles_corrupted_scene,
  TestRunPipeline.test_handles_corrupted_file) - see item 1

- conftest.py streamlining - DONE 2026-07-29, matches the 2026-07-28 scoping almost exactly:
    - `make_config(**overrides)` (factory fixture: builds real tmp_path-backed files/dirs, merges
      `**overrides` into a valid base dict, returns `PipelineConfig(**merged)`) now lives in conftest.py and
      is shared by test_cli.py, test_run_pipeline.py, test_model_bundle.py, test_detection.py - exactly the
      "dict pretending to be a config" problem this was scoped to fix, now that all three are real
      PipelineConfig instances from one source of truth instead of divergent dicts. Because PipelineConfig
      (and every nested model) is frozen, tests needing a different value per-test (e.g. mode.do_3d) call
      make_config(mode={"do_3d": True}) to get a fresh instance rather than mutating a shared one - confirmed
      by hitting a real pydantic.ValidationError ("Instance is frozen") when mutation was tried directly.
    - `make_stack(shape)` (factory fixture: np.random.rand(*shape).astype(np.float32)) replaced the fixed-size
      stack_2d/stack_3d fixtures in both test_detection.py and test_segmentation.py, exactly the factory
      approach scoped below - also incidentally resolved the stale "40x40" docstring noted below, since the
      fixed-size fixtures (and their docstrings) no longer exist.
    - `base_params` in test_detection.py vs test_object_measurement.py confirmed still unrelated (different
      call signatures for different functions under test) and deliberately left local, not merged - same
      false-friend risk as the old base_config pair, just never had a reason to converge like base_config did.

5. code organization
- config.py has no schema/validation, KeyErrors surface far from the actual mistake
  <- IN PROGRESS, starting 2026-07-28: migrating to pydantic (`uv add pydantic` not yet run). Scoped
     2026-07-27, see LEARNING_NOTES.md for the full design discussion. Plan:
       - load_config() will return a PipelineConfig model (not a plain dict + .model_dump()) - real
         attribute-access rewrite, not just boundary validation. Every `config: dict` signature becomes
         `config: PipelineConfig`, every `config["section"]["key"]` becomes `config.section.key`.
       - full call-site inventory (grep-verified 2026-07-27): cli.py (1 site), qc_figures.py (1, inside
         _panel_ecdf - consider passing prob_thresh: float directly instead of the whole config, since
         that's the only field it needs), run_pipeline.py (7 sites), utils.py (2 sites, ModelBundle).
         Test fixtures needing PipelineConfig instead of raw dicts: test_cli.py, test_run_pipeline.py,
         test_model_bundle.py, test_config.py.
       - recommended build order: config.py's PipelineConfig schema + test_config.py first (in isolation)
         -> cli.py -> utils.py -> run_pipeline.py -> qc_figures.py -> remaining tests.
       - config.yml structure decisions (all settled 2026-07-27):
           - channels.misc: DROP - confirmed zero references anywhere in src/ (grepped); was historically
             used for a ground-truth fluorescent marker channel, not needed now.
           - segmentation.use_gpu: was dead (utils.py hardcoded gpu=True) AND actively misdocumented -
             PKG-INFO/README claims "GPU use is optional and controlled per-run via segmentation.use_gpu"
             which was false. FIXED 2026-07-27 in utils.py (_load_cellpose now reads
             config["segmentation"]["use_gpu"]) - will need rewriting again to config.segmentation.use_gpu
             once PipelineConfig lands. NOTE: this fix currently makes
             test_model_bundle.py::TestLoadCellpose::test_calls_cellpose_models_with_correct_path fail
             with KeyError('segmentation') - expected/known, the dict fixture predates the fix and is
             getting rewritten to PipelineConfig anyway, not worth a throwaway patch first.
           - mode.do_3d: staying nested under `mode:` as-is (no strong reason to flatten - "doesn't matter"
             per discussion, defaulted to the zero-diff option).
       - typing choices for the new schema: paths.raw_data_dir / paths.cellpose_models_path /
         paths.spotiflow_models_path -> pydantic DirectoryPath (fails fast at load time if the path
         doesn't exist, instead of failing confusingly deep inside ModelBundle.load()/BioImage() later);
         paths.out_dir stays plain Path (pipeline creates it via mkdir, shouldn't need to pre-exist);
         detection.prob_thresh / segmentation.stitch_threshold -> Field(gt=0, lt=1) (probability-like,
         unbounded today); segmentation.bin_factor / detection.min_distance / channels.* -> Field(ge=0).
- experiment name inferred from folder structure (run_pipeline.py), can silently end up empty
- obejct_measurement.py filename typo is existing/intentional, don't fix blindly

  RESOLVED 2026-07-28 (was open in "Questions to follow up on"): qc_figures.py's _panel_ecdf /
  make_qc_figure will take prob_thresh: float directly instead of the whole config/PipelineConfig -
  narrows the signature to what the function actually depends on, decouples a plotting helper from the
  full config schema. Folded into the call-site rewrite for that file (step 5 of the build order above).

  IMPLEMENTATION STATUS 2026-07-28: config.py's PipelineConfig schema is written and DONE, including the
  use_default_model restructuring below (ModeConfig, PathsConfig, ChannelConfig, SegmentationConfig,
  DetectionConfig, all frozen via ConfigDict(frozen=True) - including the 4 nested models, not just
  PipelineConfig). config.yml updated to match (cellpose_model_path/spotiflow_model_path under
  segmentation:/detection:, singular naming). test_config.py DONE (44 tests: TestYAML, TestDefaultModels
  LoadLogic, TestFieldConstraints, TestDefaults, TestPathValidation, TestRequiredFields, TestFrozen - full
  schema coverage including the FilePath/DirectoryPath wrong-kind-of-path cases that would have caught the
  cellpose_model_path bug).

  DONE 2026-07-29: the call-site rewrite - cli.py, utils.py/ModelBundle, run_pipeline.py, qc_figures.py all
  migrated from config["section"]["key"] to config.section.key (grep-verified: zero `config[` occurrences
  left anywhere in src/spot_detector/). qc_figures.py's _panel_ecdf/make_qc_figure narrowed to
  prob_thresh: float per the resolved decision above. ModelBundle.load() dropped its redundant separate
  do_3d argument (config.mode.do_3d is read internally now) - call site in run_pipeline.py simplified to
  match. Test fixtures rewritten to match: test_cli.py, test_run_pipeline.py, test_model_bundle.py all now
  build real PipelineConfig instances via a shared make_config factory fixture in conftest.py (see item 4).
  Two real bugs caught by actually running the tests, not just reading the diff, during this rewrite:
    - qc_figures.py _panel_ecdf: a stray trailing comma survived the config["detection"]["prob_thresh"] ->
      config.detection.prob_thresh rewrite inside an f-string brace (f"thresh={config.detection.prob_thresh,}"),
      silently turning the interpolated value into a 1-tuple (renders as "thresh=(0.3,)") - syntactically
      valid Python, invisible to pytest/type-checkers, only visible by reading the rendered QC figure legend.
    - test_model_bundle.py: a fixture named mock_validate_passtrhough (patches ModelBundle.
      _validate_spotiflow_mode via mocker.patch.object, side_effect=lambda model, config: model) was being
      CALLED in three test bodies (mock_validate_passtrhough()) as if it needed "activating" - but a
      mocker.patch fixture is already active the moment pytest resolves it as a dependency. Calling the
      returned Mock invokes its side_effect with zero args, raising TypeError: missing 2 required positional
      arguments. Fix was deleting the erroneous call, not changing the fixture.
  STILL NOT IMPLEMENTED: the "flag wins" use_default_model precedence logic itself (see the design decision
  below) - ModelBundle/_load_cellpose/_load_spotiflow_from_config load whatever path is configured
  unconditionally and never check use_default_model at all.

  DESIGN ADDITION 2026-07-28, not yet coded - "run with no custom models" support:
  - cellpose_models_path / spotiflow_models_path move OUT of PathsConfig, into SegmentationConfig /
    DetectionConfig respectively (co-located with each model's other settings), becoming
    Optional[DirectoryPath] = None.
  - each of those two sections gets a new use_default_model: bool = False field, plus a LOCAL
    @model_validator(mode="after") enforcing "path must be set unless use_default_model is True" - kept
    local to each section (not a cross-section PipelineConfig-level validator) since the path and its flag
    now live in the same model.
  - two independent flags, not one shared paths.use_default_models bool - rejected the single-flag version
    because the project already hit a real case of "custom model for one stage, not the other" (the
    do_3d/spotiflow_models_path mismatch bug from the 2026-07-27 GPU profiling session, item 3 above).
  - default False (not True): forces an explicit opt-in to the pretrained fallback rather than a bare/
    incomplete config silently defaulting to a generic model - the "be vocal about defaults" requirement.
    Cellpose has its own robust pretrained default (no fallback code needed on our side); Spotiflow's
    fallback chain already exists in utils.py (ModelBundle) and already logs when it triggers - that logging
    is what makes the default "vocal", not the schema itself. See LEARNING_NOTES.md 2026-07-28 for the full
    reasoning trail.
  - PathsConfig shrinks to just raw_data_dir + out_dir once this lands.
  - follow-up once implemented: config.yml needs the two path keys moved under segmentation:/detection:,
    and CLAUDE.md's Config schema paragraph needs updating again to match (currently still describes the
    pre-restructure paths.{...} shape, flagged there as "mid-restructure").

  RESOLVED 2026-07-28: "flag wins" semantics for use_default_model when BOTH a path AND
  use_default_model=True are present in config. The schema alone only guarantees the path is optional when
  the flag is True - it says nothing about which model actually gets loaded if a path is ALSO configured
  alongside the flag. Two options were: (a) flag wins - True always loads the pretrained default, configured
  path gets ignored (and should be logged as ignored); (b) path wins - a present, valid path is always used
  regardless of the flag, so use_default_model only matters when the path is absent. Picked (a): makes the
  flag an actual behavioral switch (useful for e.g. temporarily A/B-testing against the default without
  deleting a configured path) rather than "path is allowed to be missing," which is a weaker knob than
  designed.

  DONE 2026-07-30: implemented in utils.py's _load_cellpose and _load_spotiflow_from_config. Both now check
  use_default_model explicitly BEFORE attempting the configured path (guard clause: if not use_default_model,
  handle the custom-path case and return; otherwise fall through to the default-loading path), rather than
  relying on the configured path happening to fail. Each logs a warning (via logger.warning) when a path was
  configured but is being ignored - no warning fires when use_default_model=True and no path was set, since
  there's nothing to ignore. _load_cellpose omits the pretrained_model kwarg entirely to reach Cellpose's own
  built-in default (verified via inspect.signature: CellposeModel.__init__'s pretrained_model defaults to
  'cpsam_v2' when omitted - a real, sane default, not None/error). _load_spotiflow_from_config's
  use_default_model=True branch goes straight to Spotiflow.from_pretrained(fallback) and never calls
  Spotiflow.from_folder at all - genuinely distinct from the pre-existing exception-driven fallback path
  (custom load attempted, raises, caught, falls back), both now separately tested.
  Test coverage added (test_model_bundle.py): TestLoadCellpose.test_flag_precedence and
  TestLoadSpotiflow.test_flag_wins_behavior, each parametrized over
  (use_default_model/configure_path[, do_3d, expected_fallback]) - 3 cases apiece, 6 new tests total (107 ->
  113). The Spotiflow test's key assertion is mock_from_folder.assert_not_called() - proving the custom path
  was never attempted, not just that it happened to fail, which is the actual behavioral difference "flag
  wins" introduces. The warning-log assertion was folded into the same parametrized test rather than a
  separate one, since configure_path already varies True/False across the existing cases - a separate test
  would've just rebuilt the same config permutations to check one more mock call.
  Two real bugs caught by running the tests, not just reading the diff:
    - test_model_bundle.py: a new Spotiflow-focused test accidentally called ModelBundle._load_cellpose
      instead of _load_spotiflow (copy-paste from the cellpose version of the test) - surfaced as a real
      RuntimeError from torch.load/mmap, because _load_cellpose then tried to construct an actual
      CellposeModel against make_config's dummy (empty, .touch()'d) cellpose_model.ckpt file, since nothing
      in that test mocked CellposeModel itself. Caught by actually running the tests (3 parametrize cases
      failed with the same traceback), not by reading the test body.
    - utils.py: _load_spotiflow_from_config's default-model debug log said "Loading default Cellpose
      model..." (copy-paste from _load_cellpose) instead of "Spotiflow" - cosmetic, but misleading in logs.
  Item 5 (pydantic config migration) is now fully closed - schema, call-site rewrite, and flag-wins
  precedence logic are all done and tested.

6. segmentation correctness   <- BUG CONFIRMED 2026-07-28, not yet fixed (found while pydantic-validating
   bin_factor, deliberately deferred to stay scoped to the config migration)
- segment_2d/segment_3d (segmentation_detection.py): block_reduce() does NOT require bin_factor to evenly
  divide the image shape - it silently zero-pads up to the next multiple before reducing (verified:
  skimage.measure.block_reduce on a (513,513) image with factor=4 returns (129,129), not an error).
  masks.repeat(factor, ...) then scales back up by exactly `factor`, landing on the PADDED size (516,516),
  not the original (513,513) - masks_cleaned ends up larger than bf_stack whenever image dims aren't evenly
  divisible by bin_factor. No crash anywhere in the chain.
- downstream impact: assign_spots_to_mask (run_pipeline.py) indexes into this oversized `masks` using spot
  coordinates from the full-resolution, UNBINNED spot_stack - indices still land in-bounds (no crash), but
  mask content near the edges is corrupted (zero-padding got averaged into the last real block before
  Cellpose even ran) and everything downstream (edge-mask stripping, spot assignment, QC overlays) works
  off a shifted/wrong-shaped array. Silent, dimension-dependent - only bites on scenes whose shape isn't
  evenly divisible by bin_factor, so easy to miss in testing against one well-behaved image size.
- explicitly NOT something PipelineConfig/SegmentationConfig can validate: divisibility depends on each
  image's shape, which isn't known until BioImage opens the file - one config.yml runs across every
  file/scene in raw_data_dir, and they aren't guaranteed to share dimensions.
- fix belongs in segmentation_detection.py. Two real options discussed 2026-07-28, neither picked yet:
    (a) crop the upscaled mask back to original shape - masks_resized[:orig_shape] on each axis before
        edge-mask removal; simple, keeps output aligned, discards the few padded pixels (never real data)
    (b) fail fast - check image_shape[i] % factor == 0 at the top of segment_2d/segment_3d and raise
        ValueError naming the offending scene, instead of silently producing misaligned output
  leaning (a) since it's a real fix rather than just surfacing the limitation, but not decided.
---

7. code organization: split qc_figures.py   <- DONE 2026-09-04. Split executed on the panel-coverage safety
   net (all 6 _panel_* test classes green), before the 3 figure-builder classes - the ordering the user
   landed on 2026-09-03 (see the NEXT note further up), since those 3 classes stay in test_qc_figures.py
   post-split anyway and land just as easily after.
   Executed exactly as scoped below: src/spot_detector/qc_panels.py (715 lines) = SpotData, ImageData,
   _flow_to_rgb, all 6 _panel_* helpers, verbatim (diff-verified: panel bodies unchanged, only section-
   header comments differ). src/spot_detector/qc_figures.py (345 lines) = the 3 public make_* builders only,
   now `from spot_detector.qc_panels import (...)` at the top. run_pipeline.py's import line is UNCHANGED
   (`from spot_detector.qc_figures import make_qc_figure, ...`) - pure internal refactor, zero caller change,
   grep-confirmed no other src/ module references any moved name. Tests mirror 1:1: tests/test_qc_panels.py
   (980 lines at the point of the split, all 9 panel/dataclass classes - see item 8 for the subsequent
   readability pass that brought this down to 803) + tests/test_qc_figures.py (now just the 3 builder stub
   classes; its now-unused import block was stripped - all imports were dead once only the `pass` stubs
   remained). Full suite green (177), ruff check + format clean. CLAUDE.md updated: "Key modules" bullet
   now describes both files + the public/private cut rationale, Testing conventions paragraph names
   test_qc_panels.py and moves the "first nested test classes" note onto it. AGREED 2026-09-03 details:
   qc_figures.py was 1030 lines, the largest module in the repo.
   The cut (clean because everything except the 3 public make_* builders is `_`-prefixed / private):
     - qc_figures.py STAYS as the public API: make_qc_figure, make_scene_summary_figure,
       make_run_summary_figure (the only names run_pipeline.py imports - callers see zero change), imports
       what it needs from the new module.
     - qc_panels.py (new): SpotData, ImageData, _flow_to_rgb, and all 6 _panel_* helpers (~680 lines).
   Tests mirror it 1:1 (keeps the repo's "one test file per source module" convention):
     - test_qc_panels.py: TestSpotData, TestImageData, TestFlowToRGB, TestPanelSegemntation,
       TestPanelSpotDetection, TestPanelFlow, TestPanelZDistribution, TestPanelECFD, TestPanelSpotmap.
     - test_qc_figures.py: TestMakeQCFigure, TestMakeSceneSummaryFigure, TestMakeRunSummaryFigure.
   Rationale (see also LEARNING_NOTES.md 2026-09-03): split on the visibility boundary = pure internal
   refactor, no other module's imports change, lowest-risk kind. The stronger driver is the TEST file (a
   ~800-line test module is hard to navigate); the source module is cohesive (one responsibility, well
   sectioned) so its length alone is a weak smell. Deliberately NOT splitting tests without splitting
   source - that would break the one-file-per-module mapping.
   FOLLOW-UP once done: CLAUDE.md's Architecture section ("Key modules under src/spot_detector/") and the
   fan-out diagram's qc_figures.* references need updating to name qc_panels.py; the Testing conventions
   paragraph's file list too.
---

8. test_qc_panels.py readability pass   <- DONE 2026-09-04 (learning-mode: user wrote every fixture, Claude
   audited candidates first, then reviewed each edit and re-audited at the end). Done in the same sitting
   as item 7, before the 3 figure-builder classes, per the ordering agreed 2026-09-03.
   Net effect: tests/test_qc_panels.py 980 -> 803 lines (-177, matches the original "~180 lines" estimate).
   Full suite green (177), ruff check + ruff format clean throughout.
   5 of the 6 candidates scoped below got a `@pytest.fixture` returning a dict of valid default args, with
   individual tests overriding one or two keys in-place before calling (same shape TestIs3dTrue's pre-
   existing `valid_3d` fixture already used):
     - TestPanelSegemntation -> `valid_segmentation` (not originally scoped below, caught during the pass -
       same repeated-SimpleNamespace pattern as the others).
     - TestPanelSpotDetection -> `valid_spot_detection`.
     - TestPanelECFD -> `valid_ecdf`.
     - TestPanelSpotmap -> `valid_spotmap`.
     - TestPanelZDistribution.TestIs3dFalse -> `valid_zdist_2d`.
   The separately-scoped `images=... / spot_labels=...` Ellipsis-noise helper was DROPPED as its own idea -
   folding those two keys directly into `valid_zdist_2d` (as `...`) solved the same noise without a second
   `_call()` abstraction layer. Design decision made explicit during this pass: the "unused" sentinel for a
   panel argument a given branch never reads should stay `...` (Ellipsis), not `None` - `None` is a real,
   meaningful value elsewhere in this same module (`images.masks_2d`, `spots.z_um`, `spots.x`/`y` when
   `has_spots=False`), so using it here for "truly not read" would read as an intentional null and invite a
   future reader to go check for an `is None` branch that doesn't exist. `TestPanelFlow` was looked at and
   deliberately left alone - each of its 8 tests' `flow_details` genuinely differs in shape/content (that
   variation IS the test), so a shared fixture would mostly just get overridden away with near-zero payoff.
   Two real bugs caught during this pass, both the same shape - an `ax.<method>.assert_not_called()` sitting
   *inside* a `with pytest.raises(FatalPipelineError):` block instead of after it, making it dead code (the
   raised exception exits the block before that line executes, so the test still passes but proves nothing
   about "does the trailing code get skipped"):
     - TestPanelECFD.test_fatal_pipeline_error_propagates_uncaught - PRE-EXISTING (predates this pass, and
       predates the qc_figures.py/qc_panels.py split too - todo.txt's own 2026-09-03 entry for this test
       claims it was "fixed 2026-09-03: ... dedented to sit after the block", but the fix evidently didn't
       survive whatever came next; this pass is what actually fixed it, now dedented for real, verified by
       re-grepping every `with pytest.raises(FatalPipelineError)` block in the file for consistent indent).
     - TestPanelSpotmap.test_fatal_pipeline_error_propagates_uncaught - a NEW regression introduced by this
       same pass's own fixture rewrite (diff-confirmed: correctly dedented pre-refactor, re-indented back
       inside the `with` block when the test was rewritten to use `valid_spotmap`). Caught and fixed in the
       same sitting via a targeted re-check (grepped every `with pytest.raises(FatalPipelineError)` site and
       its trailing lines) before the pass was called done - a good example of why "did the fixture change
       preserve behavior" needs an explicit re-check pass, not just "tests still pass" (a dead assertion
       doesn't fail the suite either way).
---
